Re-usable components with props
Defining components templates
- There are many ways in displaying the template
- One method is the x-template way.
- The template is linked to the component function by id.
- The template must contaian one root element.